# Bacheca offerte

Contenuto cifrato lato client (AES-GCM 256, chiave derivata con PBKDF2).
Senza la password, qui non c'e' nulla di leggibile.

Aggiornata automaticamente dal repo privato `job-hunt`.
